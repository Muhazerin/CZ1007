/*#include <stdio.h>
#include <math.h>
int main()
{
    double a1, a2, b1, b2, c1, c2, x, y, deno, xNumer, yNumer;

    puts("Enter the values for a1, b1, c1, a2, b2, c2:");
    scanf("%lf %lf %lf %lf %lf %lf", &a1, &b1, &c1, &a2, &b2, &c2);

    deno = a1*b2 - a2*b1;
    if(deno == 0) {
        puts("Unable to compute because the denominator is zero!");
        return 1;
    }
    xNumer = b2*c1 - b1*c2;
    yNumer = a1*c2 - a2*c1;
    x = xNumer / deno;
    y = yNumer / deno;

    printf("x = %.2lf and y = %.2lf\n", x, y);

    return 0;
}
*/
