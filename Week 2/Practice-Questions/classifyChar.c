/*#include <stdio.h>
int main()
{
    char ch;
    printf("Enter a character: \n");
    scanf("%c", &ch);

    if (ch >= 'A' && ch <= 'Z')
        puts("Upper case letter");
    else if (ch >= 'a' && ch <= 'z')
        puts("Lower case letter");
    else if (ch >= '0' && ch <= '9')
        puts("Digit");
    else
        puts("Special character");

    return 0;
}
*/
